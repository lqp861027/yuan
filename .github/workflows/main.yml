name: IPTV Allon:push:schedule:- name: daily_taskdescription: Run task every day at 4:00 PMcron: "0 16 * * *"branches:- mainjobs:build:runs-on: ubuntu-lateststeps:- name: Checkout codeuses: actions/checkout@v2- name: Set up Pythonuses: actions/setup-python@v2with:python-version: 3.x- name: Install dependenciesrun: pip install selenium requests- name: Install Chrome WebDriverrun: |LATEST_CHROMEDRIVER_VERSION=120.0.6099.109curl -sS -o chromedriver_linux64.zip "https://edgedl.me.gvt1.com/edgedl/chrome/chrome-for-testing/120.0.6099.109/linux64/chrome-headless-shell-linux64.zip"sudo unzip chromedriver_linux64.zip -d /usr/local/binrm chromedriver_linux64.zip- name: Set chromedriver pathrun: |sudo ln -sf /usr/local/bin/chrome-headless-shell-linux64/chrome-headless-shell /usr/local/bin/chromedriversudo chmod +x /usr/local/bin/chromedriver- name: Run scriptrun: python ${{ github.workspace }}/IPTV.py- name: 提交更改run: |git config --local user.email "你自己的github邮箱"git config --local user.name "你自己的github用户名"git add .git commit *.txt -m "Add generated file"git push -f
